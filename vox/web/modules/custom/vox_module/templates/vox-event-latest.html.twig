<div>
  <h5>
    Event
  </h5>
  {% for node in data %}
  <div id="event">
    <img src="{{ file_url(node.field_event_image.entity.fileuri) }} " class="event-img">
    <div class="event-desc">
      <h5>
        {{ node.field_event_date.value | date("d F Y") }}
      </h5>
      <p>
        {% set text = node.field_event_description.value|striptags %}
        {{ text|length > 300 ? text|slice(0, 300)|raw ~ '...' : text|raw }}
      </p>
      <a href="/node/{{ node.id }}">
        detail
      </a>
    </div>
  </div>
  {% endfor %}
</div>
